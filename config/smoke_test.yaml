model:
  size: 'XS'

data:
  name: 'cifar10'
  image_size: 32

training:
  epochs: 2
  batch_size: 4
  learning_rate: 0.0001
  num_timesteps: 100
  max_d: 5
  limit_train_batches: 10
  optimizer:
    beta1: 0.9
    beta2: 0.95
    weight_decay: 0.02

carbon_stop:
  enabled: false
