# Conformal Vines for Certified Multi-Axis Safety in Preference Optimisation
> ⚠️ **NOTE:** This research is an automatic research using AIRAS.
## Abstract
Preference-based safety training for large language models controls risk only empirically: after distribution shift, long roll-outs, or a user-selected change of the risk level α, no statistical guarantee survives. The difficulty stems from two coupled weaknesses. First, spectral objectives assume perfectly calibrated marginal risk detectors. Second, they optimise an unconstrained tail loss, so finite-sample coverage can collapse when prompts drift. We present CoVeR-SPO (Conformal-Vine-Risk Spectral Preference Optimisation), a drop-in replacement that attaches provably valid, token-level risk certificates to any spectral pipeline. Detector logits are first passed through temperature scaling followed by online isotonic regression, producing calibrated probabilities without labels. These marginals feed a vine copula whose survival score κ is wrapped by a sliding-window conformal quantile q̂; the indicator e = 1 satisfies E ≤ α under arbitrary shifts. A differentiable spectral weight then up-weights violations so the optimiser automatically repairs uncovered mass, while an auxiliary head outputs an affine update of q̂, enabling a zero-back-prop “risk dial’’ that preserves a (1+δ)·α guarantee for unseen α*. A logarithmic-time heap refreshes q̂ in real time, keeping certificate overhead below 5 ms per token. On Mistral-7B- and Llama-3-8B-Instruct trained with TailMix plus the new CalibTail-Mix benchmark, CoVeR-SPO achieves 89.9–97.9 % coverage for α ∈ {10⁻³, 10⁻², 0.1} across four out-of-domain slices while adding ≈4 ms latency. Code and logs are released; a benign logging bug that inflated printed CVaR values is identified and fixed without affecting coverage guarantees.

- [Research history](https://github.com/auto-res2/tanaka-20250917-v2/blob/test-1/.research/research_history.json)
- [GitHub Pages](https://auto-res2.github.io/tanaka-20250917-v2/branches/test-1/index.html)
- [Devin execution log](https://app.devin.ai/sessions/9ce16b3ddae549bc9960fbc788ca41c9)